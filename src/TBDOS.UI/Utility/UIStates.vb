Friend Module UIStates
    Friend ReadOnly Title As String = NameOf(Title)
    Friend ReadOnly MainMenu As String = NameOf(MainMenu)
    Friend ReadOnly ConfirmQuit As String = NameOf(ConfirmQuit)
    Friend ReadOnly Quit As String = NameOf(Quit)
    Friend ReadOnly Options As String = NameOf(Options)
    Friend ReadOnly ScreenSize As String = NameOf(ScreenSize)
    Friend ReadOnly InPlay As String = NameOf(InPlay)
    Friend ReadOnly ConfirmAbandon As String = NameOf(ConfirmAbandon)
End Module
